class Solution {
    public int findMaxConsecutiveOnes(int[] nums) { 

        int count =0 ; 
        int maxconsec =0;
        int n = nums.length; 

        for(int i =0 ; i<n; i++){

            if(nums[i]==1){
                count++;
            }
            else{
                count =0;
            }

maxconsec = Math.max(maxconsec, count);

        }
        //maxconsec = Math.max(maxconsec, count);

return maxconsec;
        
    }
    //return maxconsec;
}
